import React from'react';import{ShieldAlert}from'lucide-react';import{useCMS}from'../context/CMSContext';import{useLanguage}from'../context/LanguageContext';
export const PrivacyPage:React.FC=()=>{const{data}=useCMS();const{language,isArabic}=useLanguage();const page:any=data.pages.find((p:any)=>p.id==='privacy'||p.slug==='privacy');const title=language==='en'?(page?.titleEn||page?.title||'Privacy Policy'):(page?.title||'سياسة الخصوصية وحماية البيانات');const content=language==='en'?(page?.contentEn||page?.content||'We are committed to protecting your privacy and personal data.'):(page?.content||'نلتزم في موقع سعيد بن عايض بحماية خصوصيتك وبياناتك الشخصية.');return <div className="min-h-screen bg-[#080808] pt-28 pb-20" dir={isArabic?'rtl':'ltr'}><div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8"><div className="p-8 rounded-3xl bg-[#121212] border border-white/10 space-y-6"><div className="flex items-center gap-3 border-b border-white/10 pb-4"><ShieldAlert className="w-6 h-6 text-[#D51F2B]"/><h1 className="text-2xl sm:text-3xl font-black text-white">{title}</h1></div><div className="text-gray-300 text-sm leading-relaxed font-light whitespace-pre-line">{content}</div></div></div></div>};