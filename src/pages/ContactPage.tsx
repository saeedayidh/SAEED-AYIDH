import React,{useState}from'react';import{Send,CheckCircle2}from'lucide-react';import{useCMS}from'../context/CMSContext';
export const ContactPage:React.FC=()=>{const{data}=useCMS();const[submitted,setSubmitted]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const cfg:any=(data.global as any).contactPage||{};const handleSubmit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);setError('');const fd=new FormData(e.currentTarget);const name=String(fd.get('name')||'').trim(),email=String(fd.get('email')||'').trim(),subject=String(fd.get('subject')||'').trim(),message=String(fd.get('message')||'').trim();try{const r=await fetch('/api/submissions',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({type:'contact',name,email,message:subject?`${subject}\n\n${message}`:message})});if(!r.ok)throw new Error();setSubmitted(true)}catch{setError('تعذر إرسال الرسالة. حاول مرة أخرى.')}finally{setLoading(false)}};return <div className="pt-32 pb-24 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 space-y-12 animate-fade-in"><div className="text-center space-y-4"><span className="text-xs font-bold text-[#D51F2B] bg-[#191919] px-4 py-1.5 rounded-full border border-white/10">{cfg.badge||'التواصل المباشر'}</span><h1 className="text-4xl sm:text-6xl font-black text-white">{cfg.title||'تواصل مع سعيد بن عايض'}</h1><p className="text-base sm:text-lg text-[#B8B8B8] font-light max-w-xl mx-auto">{cfg.description||'أرحب باستفساراتك، مقترحات المشاريع، وفرص التعاون والتغطيات.'}</p></div><div className="sba-card p-8 sm:p-12">{!submitted?<form onSubmit={handleSubmit} className="space-y-6 text-right"><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div><label className="block text-xs font-bold text-gray-300 mb-2">الاسم الكامل</label><input name="name" type="text" required className="w-full px-4 py-3.5 rounded-xl bg-[#080808] border border-white/10 text-sm text-white focus:outline-none focus:border-[#D51F2B]"/></div><div><label className="block text-xs font-bold text-gray-300 mb-2">البريد الإلكتروني</label><input name="email" type="email" required className="w-full px-4 py-3.5 rounded-xl bg-[#080808] border border-white/10 text-sm text-white focus:outline-none focus:border-[#D51F2B]"/></div></div><div><label className="block text-xs font-bold text-gray-300 mb-2">موضوع التواصل / اسم المشروع</label><input name="subject" type="text" className="w-full px-4 py-3.5 rounded-xl bg-[#080808] border border-white/10 text-sm text-white focus:outline-none focus:border-[#D51F2B]"/></div><div><label className="block text-xs font-bold text-gray-300 mb-2">التفاصيل والرسالة</label><textarea name="message" rows={5} required className="w-full px-4 py-3.5 rounded-xl bg-[#080808] border border-white/10 text-sm text-white focus:outline-none focus:border-[#D51F2B]"/></div>{error&&<p className="text-sm text-red-400">{error}</p>}<button type="submit" disabled={loading} className="sba-btn-primary w-full py-4 text-sm font-bold flex items-center justify-center gap-2 cursor-pointer disabled:opacity-50"><Send className="w-4 h-4"/><span>{loading?'جاري الإرسال...':'إرسال الرسالة'}</span></button></form>:<div className="text-center py-12 space-y-4"><CheckCircle2 className="w-16 h-16 text-emerald-500 mx-auto"/><h3 className="text-2xl font-black text-white">تم إرسال رسالتك بنجاح!</h3><p className="text-sm text-[#B8B8B8]">تم استلام الرسالة في لوحة التحكم.</p><button onClick={()=>setSubmitted(false)} className="sba-btn-secondary px-6 py-2.5 text-xs">إرسال رسالة أخرى</button></div>}</div></div>};