import React,{useState}from'react';import{Lightbulb,CheckCircle2}from'lucide-react';import{useCMS}from'../context/CMSContext';import{useLanguage}from'../context/LanguageContext';
export const SuggestionPage:React.FC=()=>{const{data}=useCMS();const{language}=useLanguage();const cfg:any=(data.global as any).suggestionPage||{};const en=language==='en';const t=(a:string,b:string,fa:string,fb:string)=>en?(cfg[b]||fb):(cfg[a]||fa);const[submitted,setSubmitted]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const handleSubmit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setLoading(true);setError('');const fd=new FormData(e.currentTarget);try{const r=await fetch('/api/submissions',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({type:'suggestion',name:String(fd.get('name')||''),email:String(fd.get('email')||''),message:String(fd.get('message')||'')})});if(!r.ok)throw new Error();setSubmitted(true)}catch{setError(t('error','errorEn','تعذر إرسال الاقتراح. حاول مرة أخرى.','Could not send your suggestion. Please try again.'))}finally{setLoading(false)}};return <div className="pt-32 pb-24 max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 space-y-12 animate-fade-in"><div className="text-center space-y-4"><div className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-[#191919] border border-white/10 text-xs font-semibold text-amber-500"><Lightbulb className="w-3.5 h-3.5"/><span>{t('badge','badgeEn','تطوير المحتوى والخدمات','Content & Services Development')}</span></div><h1 className="text-4xl sm:text-5xl font-black text-white">{t('title','titleEn','بطاقة اقتراح','Suggestion')}</h1><p className="text-base sm:text-lg text-[#B8B8B8] font-light max-w-xl mx-auto">{t('description','descriptionEn','عندك فكرة أو اقتراح يساعدني في تطوير الموقع أو المحتوى أو الخدمات؟ شاركني اقتراحك.','Have an idea that can improve the website, content or services? Share it with me.')}</p></div><div className="sba-card p-8 sm:p-12 border-amber-500/30">{!submitted?<form onSubmit={handleSubmit} className="space-y-6 text-right"><Field name="name" type="text" label={t('nameLabel','nameLabelEn','الاسم *','Name *')}/><Field name="email" type="email" label={t('emailLabel','emailLabelEn','البريد الإلكتروني *','Email *')}/><div><label className="block text-xs font-bold text-gray-200 mb-2">{t('messageLabel','messageLabelEn','الرسالة والاقتراح *','Suggestion details *')}</label><textarea name="message" rows={5} required className="w-full px-4 py-3.5 rounded-xl bg-[#080808] border border-white/10 text-sm text-white focus:outline-none focus:border-[#D51F2B]"/></div>{error&&<p className="text-sm text-red-400">{error}</p>}<button type="submit" disabled={loading} className="sba-btn-primary w-full py-4 text-sm font-bold cursor-pointer disabled:opacity-50">{loading?t('loading','loadingEn','جاري إرسال الاقتراح...','Sending...'):t('submitLabel','submitLabelEn','إرسال اقتراح','Send suggestion')}</button></form>:<div className="text-center py-12 space-y-4"><CheckCircle2 className="w-16 h-16 text-emerald-500 mx-auto"/><h3 className="text-2xl font-black text-white">{t('successTitle','successTitleEn','تم إرسال اقتراحك بنجاح!','Suggestion sent successfully!')}</h3><p className="text-sm text-[#B8B8B8]">{t('successText','successTextEn','تم حفظ الاقتراح في لوحة التحكم.','Your suggestion has been saved.')}</p><button onClick={()=>setSubmitted(false)} className="sba-btn-secondary px-6 py-2.5 text-xs">{t('againLabel','againLabelEn','إرسال اقتراح آخر','Send another')}</button></div>}</div></div>};
const Field=({name,type,label}:{name:string;type:string;label:string})=><div><label className="block text-xs font-bold text-gray-200 mb-2">{label}</label><input name={name} type={type} required className="w-full px-4 py-3.5 rounded-xl bg-[#080808] border border-white/10 text-sm text-white focus:outline-none focus:border-[#D51F2B]"/></div>;
