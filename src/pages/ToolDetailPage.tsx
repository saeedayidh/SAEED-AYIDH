import React,{useState}from'react';
import{useParams,Link}from'react-router-dom';
import{useCMS}from'../context/CMSContext';
import{SEO}from'../components/SEO';
import{Breadcrumbs}from'../components/Breadcrumbs';
import{MediaSystem}from'../components/MediaSystem';
import{Wrench,Copy,Check,Download,ShieldCheck,Cpu}from'lucide-react';
export const ToolDetailPage:React.FC=()=>{const{slug}=useParams<{slug:string}>();const[copied,setCopied]=useState(false);const{data}=useCMS();const list:any[]=(data.tools||[])as any[];const tool:any=list.find(t=>t.slug===slug||t.id===slug);if(!tool)return <div className="min-h-screen bg-[#080808] pt-32 text-center text-white">الأداة غير متاحة</div>;const relatedTools=list.filter(t=>t.id!==tool.id&&t.category===tool.category).slice(0,3);const copy=()=>{if(tool.promptText){navigator.clipboard.writeText(tool.promptText);setCopied(true);setTimeout(()=>setCopied(false),2000)}};return <div className="min-h-screen bg-[#080808] pt-28 pb-20"><SEO title={tool.name} description={tool.description||tool.name} image={tool.image}/><div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8"><Breadcrumbs items={[{label:'أدوات سعيد',link:'/resources'},{label:tool.name}]}/><div className="relative rounded-3xl overflow-hidden bg-gradient-to-r from-[#180305] via-[#100203] to-[#080808] border border-white/10 p-6 sm:p-8 mb-8"><div className="flex flex-col md:flex-row items-center justify-between gap-6"><div className="space-y-3 text-right"><span className="px-3.5 py-1 rounded-full bg-[#D51F2B]/15 border border-[#D51F2B]/30 text-xs font-semibold text-[#D51F2B] inline-flex items-center gap-1.5"><Wrench className="w-3.5 h-3.5"/>{tool.category}</span><h1 className="text-2xl sm:text-4xl font-black text-white">{tool.name}</h1><p className="text-gray-300 text-sm sm:text-base font-light">{tool.description}</p></div>{tool.image&&<img src={tool.image} alt={tool.name} className="w-32 h-32 object-cover rounded-2xl border border-white/10 shadow-lg shrink-0"/>}</div></div>{tool.promptText&&<div className="p-6 rounded-2xl bg-[#121212] border border-[#D51F2B]/30 space-y-4 text-right mb-8"><h3 className="text-sm font-bold text-white">النص والأمر (Prompt)</h3><div className="p-4 rounded-xl bg-[#0a0a0a] border border-white/10 font-mono text-xs text-gray-200 leading-relaxed text-left overflow-x-auto select-all">{tool.promptText}</div><div className="flex justify-end"><button onClick={copy} className="inline-flex items-center gap-2 px-6 py-2.5 rounded-xl bg-[#D51F2B] text-white text-xs font-bold">{copied?<Check className="w-4 h-4"/>:<Copy className="w-4 h-4"/>}{copied?'تم النسخ بنجاح!':'نسخ البرومبت'}</button></div></div>}{tool.filterBeforeAfter&&<div className="p-6 rounded-2xl bg-[#121212] border border-white/10 space-y-4 text-right mb-8"><h3 className="text-sm font-bold text-white">معاينة قبل وبعد</h3><div className="grid grid-cols-1 sm:grid-cols-2 gap-4"><img src={tool.filterBeforeAfter.before} alt="قبل" className="w-full h-48 object-cover rounded-xl border border-white/10"/><img src={tool.filterBeforeAfter.after} alt="بعد" className="w-full h-48 object-cover rounded-xl border border-[#D51F2B]"/></div></div>}{tool.wallpaperResolution&&<div className="p-6 rounded-2xl bg-[#121212] border border-white/10 space-y-3 text-right mb-8"><h3 className="text-sm font-bold text-white">دقة وتفاصيل الخلفية</h3><p className="text-xs text-gray-300">الدقة الحالية: <span className="font-bold text-[#D51F2B]">{tool.wallpaperResolution}</span></p>{tool.downloadUrl&&<a href={tool.downloadUrl} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-6 py-2.5 rounded-xl bg-[#D51F2B] text-white text-xs font-bold"><Download className="w-4 h-4"/>تنزيل الملف</a>}</div>}<div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8 text-right">{tool.whatItDoes&&<div className="p-6 rounded-2xl bg-[#121212] border border-white/10 space-y-3"><h3 className="text-sm font-bold text-white flex items-center gap-2"><Cpu className="w-4 h-4 text-[#D51F2B]"/>ماذا تقدم هذه الأداة؟</h3><p className="text-xs sm:text-sm text-gray-300 leading-relaxed">{tool.whatItDoes}</p></div>}{(tool.compatibility?.length||tool.requirements?.length)&&<div className="p-6 rounded-2xl bg-[#121212] border border-white/10 space-y-3"><h3 className="text-sm font-bold text-white flex items-center gap-2"><ShieldCheck className="w-4 h-4 text-[#D51F2B]"/>التوافق والمتطلبات</h3>{tool.compatibility?.length>0&&<p className="text-xs text-gray-300">الأجهزة المتوافقة: {tool.compatibility.join(' • ')}</p>}{tool.requirements?.length>0&&<p className="text-xs text-gray-300">المتطلبات: {tool.requirements.join(' • ')}</p>}</div>}</div>{(tool.howToUse?.length||tool.shortcutSetupGuide?.length)&&<div className="p-6 rounded-2xl bg-[#121212] border border-white/10 space-y-4 text-right mb-8"><h3 className="text-sm font-bold text-white">طريقة الاستخدام والخطوات</h3><ol className="space-y-2 text-xs sm:text-sm text-gray-300 list-decimal list-inside">{(tool.howToUse||tool.shortcutSetupGuide).map((step:string,idx:number)=><li key={idx} className="p-2.5 rounded-lg bg-[#181818] border border-white/5">{step}</li>)}</ol></div>}<MediaSystem galleryImages={tool.screenshots} videos={tool.videoTutorial?[tool.videoTutorial]:undefined} externalLinks={tool.externalLinks} downloads={tool.downloadUrl?[{title:`تنزيل ${tool.name}`,url:tool.downloadUrl}]:undefined}/>{relatedTools.length>0&&<div className="mt-16 pt-10 border-t border-white/10 text-right space-y-6"><h3 className="text-xl font-bold text-white">أدوات ذات صلة</h3><div className="grid grid-cols-1 sm:grid-cols-3 gap-4">{relatedTools.map((rel:any)=><Link key={rel.id} to={`/tools/${rel.slug||rel.id}`} className="group rounded-xl p-4 bg-[#121212] border border-white/10 hover:border-[#D51F2B] space-y-3">{rel.image&&<img src={rel.image} alt={rel.name} className="w-full h-32 object-cover rounded-lg"/>}<span className="text-[11px] text-[#D51F2B] font-semibold">{rel.category}</span><h4 className="text-xs font-bold text-white">{rel.name}</h4></Link>)}</div></div>}</div></div>};