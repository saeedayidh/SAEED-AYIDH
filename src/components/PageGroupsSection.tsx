import React from'react';import{Link}from'react-router-dom';import{ExternalLink}from'lucide-react';import{useCMS}from'../context/CMSContext';
export const PageGroupsSection:React.FC=()=>{const{data}=useCMS();const groups:any[]=(((data.global as any).pageGroups)||[]).filter((x:any)=>x.enabled!==false).sort((a:any,b:any)=>(a.order||0)-(b.order||0));if(!groups.length)return null;return <section className="border-t border-white/5 bg-[#080808] py-20" dir="rtl"><div className="mx-auto max-w-7xl space-y-14 px-5">{groups.map(g=><div key={g.id}><h2 className="mb-6 text-3xl font-black text-white sm:text-4xl">{g.title}</h2><div className="grid gap-4 md:grid-cols-2">{(g.items||[]).filter((x:any)=>x.enabled!==false).map((x:any)=>{const external=/^https?:\/\//i.test(x.url||'');const body=<div className="h-full rounded-2xl border border-white/10 bg-[#111] p-6 transition hover:border-[#D51F2B]/60"><div className="flex items-center justify-between gap-3"><h3 className="text-lg font-black text-white">{x.title}</h3>{x.url&&<ExternalLink className="h-4 w-4 text-[#D51F2B]"/>}</div>{x.text&&<p className="mt-3 whitespace-pre-line text-sm leading-7 text-gray-400">{x.text}</p>}</div>;return x.url?(external?<a key={x.id} href={x.url} target="_blank" rel="noreferrer">{body}</a>:<Link key={x.id} to={x.url}>{body}</Link>):<div key={x.id}>{body}</div>})}</div></div>)}</div></section>};
